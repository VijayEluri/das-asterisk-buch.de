<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN"
"http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd">
<section id="applications-realtime" lang="en" revision="$Revision$">
  <!--
% Copyright (c) 2006 - 2008 by 
% Stefan Wintermeyer <stefan.wintermeyer@amooma.de>
% Philipp Kempgen <philipp.kempgen@amooma.de>
% Permission is granted to copy, distribute and/or modify this document
% under the terms of the GNU Free Documentation License, Version 1.2
% or any later version published by the Free Software Foundation
% with no Invariant Sections, no Front-Cover Texts, and no Back-Cover
% Texts. A copy of the license is included in the section entitled "GNU
% Free Documentation License".
% Asterisk training and consulting is offered at http://www.amooma.de
-->

    <title><literal>RealTime()</literal></title>

    <indexterm significance="preferred">
      <primary>Dialplan Applications</primary>

      <secondary><code>RealTime()</code></secondary>
    </indexterm>

    <simpara>Gets configuration information from the realtime configuration
    database.</simpara>

    <synopsis><command>RealTime(<replaceable>family</replaceable>,<replaceable>column</replaceable>,<replaceable>value</replaceable>[,<replaceable>prefix</replaceable>])</command></synopsis>

    <simpara>Retrieves configuration settings from the realtime configuration
    database into channel variables. All unique column names are set as
    channel variables. Optionally, the argument
    <replaceable>prefix</replaceable> is prepended to the column name to form
    the variable name (in the example in the internal help, the prefix
    <code>var_</code> to the column name <code>test</code> results in the
    variable <code>${var_test}</code>).</simpara>

    <simpara>Sets the channel variable <code>REALTIMECOUNT</code> to the
    number of values read.</simpara>

    <para>In <filename>extconfig.conf</filename>:<programlisting>; Family =&gt; DBMS,database,table
sipusers =&gt; mysql,asterisk,sip_users</programlisting>In
    <filename>extensions.conf</filename>:<programlisting>exten =&gt; 123,1,RealTime(sipusers,ext,5678,var_)</programlisting>This
    executes the following SQL query in the database
    <code>asterisk</code>:</para>

    <simpara><code>SELECT * FROM sip_users WHERE ext = 5678</code></simpara>

    <para>Assuming the table has the columns <code>firstname</code> and<code>
    lastname</code>, we can print those values to the CLI this
    way:<programlisting>exten =&gt; 123,n,NoOp(The first name of the user at ext. 5678 is: ${var_firstname})
exten =&gt; 123,n,NoOp(The last name of the user at ext. 5678 is: ${var_lastname})</programlisting></para>

    <note>
      <formalpara>
        <title>Internal help for this application in Asterisk 1.4:</title>

        <para><screen>  -= Info about application 'RealTime' =- 

[Synopsis]
Realtime Data Lookup

[Description]
Use the RealTime config handler system to read data into channel variables.
RealTime(&lt;family&gt;|&lt;colmatch&gt;|&lt;value&gt;[|&lt;prefix&gt;])

All unique column names will be set as channel variables with optional prefix
to the name.  For example, a prefix of 'var_' would make the column 'name'
become the variable ${var_name}.  REALTIMECOUNT will be set with the number
of values read.</screen></para>
      </formalpara>

      <formalpara>
        <title>diff output to internal help in Asterisk 1.2:</title>

        <para><screen>11,14c11,13
&lt; All unique column names will be set as channel variables with optional prefix
&lt; to the name.  For example, a prefix of 'var_' would make the column 'name'
&lt; become the variable ${var_name}.  REALTIMECOUNT will be set with the number
&lt; of values read.
---
&gt; All unique column names will be set as channel variables with optional prefix to the name.
&gt; e.g. prefix of 'var_' would make the column 'name' become the variable ${var_name}
&gt; </screen></para>
      </formalpara>
    </note>

    <formalpara>
      <title>See also</title>

      <para><xref linkend="applications-realtimeupdate" /></para>
    </formalpara>
</section>

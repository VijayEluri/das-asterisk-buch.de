<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE note PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
"http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd">
<!-- AUTO-GENERATED FILE. DO NOT EDIT. -->
<note lang="de" revision="$Revision$">
  <formalpara>
    <title>Asterisk-Versionen:</title>

    <para><literallayout class="monospaced">        |     |        | <emphasis
          role="bold">1.4</emphasis> |————————| <emphasis role="bold">1.6</emphasis> |————————</literallayout></para>
  </formalpara>

  <formalpara>
    <title>Interner Hilfetext zu dieser Applikation in Asterisk 1.4:</title>

    <para><screen lang="en-US">  -= Info about application 'TryExec' =-

[Synopsis]
Executes dialplan application, always returning

[Description]
Usage: TryExec(appname(arguments))
  Allows an arbitrary application to be invoked even when not
hardcoded into the dialplan. To invoke external applications
see the application System.  Always returns to the dialplan.
The channel variable TRYSTATUS will be set to:
    SUCCESS   if the application returned zero
    FAILED    if the application returned non-zero
    NOAPP     if the application was not found or was not specified</screen></para>
  </formalpara>

  <formalpara>
    <title>Differenz des internen Hilfetexts von Asterisk 1.2 zu 1.4:</title>

    <para>-- in Asterisk 1.2 nicht vorhanden --</para>
  </formalpara>

  <formalpara>
    <title>Differenz des internen Hilfetexts von Asterisk 1.4 zu 1.6:</title>

    <para><screen lang="en-US" language="diff-u">--- in Asterisk 1.4
+++ in Asterisk 1.6
@@ -4,11 +4,11 @@
   Executes dialplan application, always returning

   [Description]
-  Usage: TryExec(appname(arguments))
-    Allows an arbitrary application to be invoked even when not
+    TryExec(appname(arguments)):
+  Allows an arbitrary application to be invoked even when not
   hardcoded into the dialplan. To invoke external applications
   see the application System.  Always returns to the dialplan.
-  The channel variable TRYSTATUS will be set to:
+  The channel variable TRYSTATUS will be set to one of:
       SUCCESS   if the application returned zero
       FAILED    if the application returned non-zero
       NOAPP     if the application was not found or was not specified</screen></para>
  </formalpara>
</note>
